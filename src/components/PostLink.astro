---
interface Props {
  post: any;
  title?: string;
}

const { post, title } = Astro.props;

const linkTitle =
  title ?? `(${post.data.pubDate.toLocaleDateString('pl-PL')}) ${post.data.title}`;
---

<p><a href={`/blog/${post.id}`} data-astro-prefetch> {linkTitle}</a></p>
