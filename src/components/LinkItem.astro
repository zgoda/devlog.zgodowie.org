---
const { name, url, description } = Astro.props;
---

<article>
  <h3><a href={url} rel="noopener noreferrer">{name}</a></h3>
  <div set:html={description} />
  <p>URL: <a href={url} rel="noopener noreferrer">{url}</a></p>
</article>
